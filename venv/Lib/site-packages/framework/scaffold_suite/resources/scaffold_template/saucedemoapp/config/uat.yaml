environment: uat
instance_url: https://uat.saucedemo.com
login_url: https://uat.saucedemo.com/
timeout_ms: 45000
wait_for_element_timeout: 7000

# Browser settings
browser: chromium
headless: true
slow_motion_ms: 0

# Test user credentials
username: ${SF_USERNAME_UAT}
password: ${SF_PASSWORD_UAT}

# Retry settings
max_retry_attempts: 3
retry_wait_ms: 500

# Application settings
app_launcher_timeout: 7000
page_load_timeout: 45000

# Optional: Proxy settings
proxy:
  enabled: false
  url: null

default_workers: auto
default_dist_scope: "loadscope"
 
report:
  unified_report_path: apps/saucedemo/reports//test_results.html   # unified report output
  batch_report_folder: apps/saucedemo/reports//batch_reports  # folder to save individual test results
 
folders:
  batch1:
      workers: "auto"
      dist_scope: "loadfile"
      testfiles:
        - "apps/saucedemo/regression/tests/tests_ui"
        - "apps/saucedemo/regression/tests//tests_api"
  batch2:
      workers: "auto"
      dist_scope: "loadfile"
      testfiles:
        - "apps/saucedemo/regression/tests/tests_db"